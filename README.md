# TICKORA

Everyday watches, engineered right. A single self-contained `index.html` — no build step, no dependencies beyond Google Fonts.

## Run locally

Open `index.html` directly in a browser, or serve the folder with any static file server.

## Structure

- `index.html` — the entire site (HTML, CSS, JS inline)
